# ML dependencies for the ml-processor service (boundary detection).
# Installed on top of the base pyproject.toml dependencies.
# PyTorch CPU-only to keep image small (~800 MB vs ~2 GB with CUDA).

--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.6.0
torchvision==0.21.0

# FTW field boundary detection toolkit (CLI + Python API)
ftw-tools>=1.4.3

# FTW transitive deps (pinned for reproducibility)
# torchgeo 0.9.0 removed AugmentationSequential which ftw-tools needs
torchgeo>=0.7.0,<0.9.0
kornia>=0.7.0
timm>=1.0.0
